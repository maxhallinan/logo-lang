(label null?
  (fn (xs)
    (equal? xs '())))

(label rember
  (fn (x xs)
    (if (null? xs)
      xs
      (if (equal? x (first xs))
        (rest xs)
        (cons
          (first xs)
          (rember x (rest xs)))))))

(rember 'b '(a b c))
