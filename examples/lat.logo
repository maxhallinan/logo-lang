(label null?
  (fn (xs)
    (equal? xs (quote ()))))

(label lat?
  (fn (xs)
    (if (null? xs)
      'true
      (if (atom? (first xs))
        (lat? (rest xs))
        '()))))

(lat? (quote (a b c)))
