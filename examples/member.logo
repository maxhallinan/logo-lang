(def null?
  (fn (xs)
    (equal? xs '())))

(def member?
  (fn (x xs)
    (if (null? xs)
      (quote t)
      (if (equal? x (first xs))
        'true
        (member? x (rest xs))))))

(member? 'c '(a b c))
